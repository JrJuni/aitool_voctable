# VOC Table 환경변수 설정 예시

# =============================================================================
# 데이터 소스 우선순위 설정
# =============================================================================
# "api_first": API 서버 우선 (공용 사용, 여러 사용자가 같은 데이터 공유)
# "local_first": 로컬 DB 우선 (개인 사용, 로컬 데이터 우선)
DATA_SOURCE_PRIORITY=api_first

# =============================================================================
# API 서버 설정
# =============================================================================
# 백엔드 API 서버 URL
API_BASE_URL=http://172.16.5.75:8000

# =============================================================================
# 데이터베이스 설정
# =============================================================================
# 로컬 MySQL 데이터베이스 설정 (API 서버 다운 시 백업용)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=voc_table

# =============================================================================
# 쿠키 보안 설정
# =============================================================================
# 쿠키 암호화 키 (운영환경에서는 반드시 변경하세요)
COOKIE_SECRET_KEY=your-secret-key-change-in-production

# =============================================================================
# 사용 예시
# =============================================================================
# 1. 공용 사용 (여러 사용자가 같은 데이터 공유):
#    DATA_SOURCE_PRIORITY=api_first
#    API_BASE_URL=http://your-api-server:8000
#
# 2. 개인 사용 (로컬 데이터 우선):
#    DATA_SOURCE_PRIORITY=local_first
#    DB_HOST=localhost
#    DB_NAME=my_voc_table
#
# 3. 하이브리드 사용 (API 우선, 로컬 백업):
#    DATA_SOURCE_PRIORITY=api_first
#    API_BASE_URL=http://your-api-server:8000
#    DB_HOST=localhost
#    DB_NAME=voc_table_backup